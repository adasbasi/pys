<br />
<br />
<div class="konum1">
<div class="konum2">
<div class="back-unit">

<h3>Giriş</h3>
<br />

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class='etiket'>
	Email :&nbsp
	<%= f.email_field :email, :autofocus => true, :placeholder => 'e-posta adresi' %>
  </div>
  <br />
  <div class='etiket'>
	Şifre :&nbsp
	  <%= f.password_field :password, :placeholder => '********' %>
   </div>
  <br />
  <% if devise_mapping.rememberable? -%>
    <div class='etiket'>
	<%= f.check_box :remember_me %>
	Remember_me
    </div>
  <% end -%>
<br />
  <div><%= f.submit "Sign in", :class => 'btn btn-primary' %></div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>
</div>
<div class="slide-position">
<div id="myCarousel" class="carousel slide">
  <!-- Carousel items -->
  <div class="carousel-inner">
    <div class="active item"><%= image_tag("13.jpg") %></div>
    <div class="item"><%= image_tag("14.jpg") %></div>
    <div class="item"><%= image_tag("15.jpg") %></div>
    <div class="item"><%= image_tag("16.jpg") %></div>
  </div>
  <!-- Carousel nav -->
  <a class="carousel-control left" href="#myCarousel" data-slide="prev">&lsaquo;</a>
  <a class="carousel-control right" href="#myCarousel" data-slide="next">&rsaquo;</a>
</div>
</div>
